# Root .env für docker-compose (nur Postgres-Container-Credentials).
# Kopieren nach .env und Werte anpassen: cp .env.example .env

POSTGRES_USER=nisorga
POSTGRES_PASSWORD=change-me
POSTGRES_DB=nisorga
